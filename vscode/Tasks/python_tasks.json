{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        // Python build tasks
        {
            "label": "run",
            "type": "shell",
            "command": "python -u ${file} < ${workspaceFolder}/.vscode/input.txt > ${workspaceFolder}/.vscode/output.txt",
            "group": {
                "kind": "build",
                "isDefault": true,
            }
        },
    ]
}
