// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// To see the default key bindings run `zed: open default keymap`
// from the command palette.
[
  {
    "context": "Workspace",
    "bindings": {
      // "shift shift": "file_finder::Toggle"
    },
  },
  {
    "context": "Editor && vim_mode == normal",
    "bindings": {
      "u": null,
      "ctrl-r": null,
    },
  },
  {
    "context": "Editor && extension == java",
    "bindings": {
      "ctrl-alt-n": ["task::Spawn", { "task_name": "Compile and Run Java" }],
    },
  },
  {
    "context": "Editor && extension == cpp",
    "bindings": {
      "ctrl-alt-n": ["task::Spawn", { "task_name": "Compile and Run C++" }],
    },
  },
  {
    "context": "Editor && extension == c",
    "bindings": {
      "ctrl-alt-n": ["task::Spawn", { "task_name": "Compile and Run C" }],
    },
  },
  {
    "context": "Editor && extension == py",
    "bindings": {
      "ctrl-alt-n": ["task::Spawn", { "task_name": "Run python" }],
    },
  },
]
